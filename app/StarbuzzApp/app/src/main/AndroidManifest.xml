<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scampa.starbuzzapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TopLevelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DrinkCategoryActivity"
            android:label="@string/title_activity_drink_category"></activity>
        <activity
            android:name=".DrinkActivity"
            android:label="@string/title_activity_drink"></activity>
        <activity
            android:name=".FoodCategoryActivity"
            android:label="@string/title_activity_food_category"></activity>
        <activity
            android:name=".FoodActivity"
            android:label="@string/title_activity_food"></activity>
        <activity
            android:name=".LimpiezaActivity"
            android:label="@string/title_activity_limpieza"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
