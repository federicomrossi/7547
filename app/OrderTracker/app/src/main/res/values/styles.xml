<resources>

    <style name="AppTheme" parent="AppTheme.Base">

    </style>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@android:color/primary_text_dark</item>

        <item name="textAppearanceLargePopupMenu">
            @style/TextAppearance.AppCompat.Widget.PopupMenu.Large
        </item>
        <item name="textAppearanceSmallPopupMenu">
            @style/TextAppearance.AppCompat.Widget.PopupMenu.Small
        </item>

    </style>

    <!-- Toolbar style -->
    <style name="CustomToolbar" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">#FFF</item>
        <item name="android:textColorSecondary">#FFF</item>
    </style>

    <!-- Launcher -->
    <style name="LauncherTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/launcher</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <item name="searchIcon">@drawable/ic_search_24dp</item>
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@android:color/white</item>
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="searchHintIcon">@null</item>
        <!-- The hint text that appears when the user has not typed anything -->
        <!--<item name="queryHint">@nullt</item>-->
    </style>

    <style name="ButtonAddToCart" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/buttonShoppingCartHighlight</item>
        <item name="colorButtonNormal">@color/buttonShoppingCart</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="ButtonConfirm" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">#B2DFDB</item>
        <item name="colorButtonNormal">#E0F2F1</item>
        <item name="android:textColor">#232323</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <style name="ButtonDiscard" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">#FF7777</item>
        <item name="colorButtonNormal">#FF5252</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <style name="NumberPickerCustom" parent="Theme.AppCompat.Light">
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorSecondary">#000000</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
